---
layout: default
title: 完整知识目录
description: 以 AI 与大模型原理为主线，保留训练知识，并行学习 RAG 与 Agent。
permalink: /roadmap/
---

# 完整知识目录

本目录定义本版手册的覆盖范围，而不是对整个 AI 学科的穷尽。主线先建立 AI 与机器学习基础，再进入大模型；Agent 独立并行，不压缩原理与训练知识。

**正文状态：**[M00 · AI 全景]({{ '/chapters/ai-foundations/' | relative_url }})与 [M01 · 数学与编程]({{ '/chapters/math-and-programming/' | relative_url }})已成稿，M01 基础实验已验证；M02–M13 规划中。列出知识点不等于已有正文、代码或验证结果。

原文线索与补充内容的区别见[覆盖与资料核验表]({{ '/coverage/' | relative_url }})。

## 学习顺序

1. **基础层：**M00 → M01 → M02 → M03。数学与编程可以随实例回补。
2. **语言模型层：**M04 → M05 → M06 → M07。先理解表示与结构，再讨论如何训练。
3. **工程层：**M08 → M09，随后深入 M10、M11、M12。
4. **案例层：**M13 随各章穿插，面试自测贯穿全部模块。

M12 可以同步学习；涉及注意力、训练、检索和评测的问题，应回到对应基础模块补全解释。

## M00. AI 全景 {#m00}

**目标：**分清研究领域、学习方法、模型、训练过程与应用系统。

- AI、机器学习、深度学习、基础模型与大语言模型的关系。
- 符号规则、搜索规划与数据驱动方法；不要把所有 AI 都等同于神经网络。
- 预测、分类、生成与决策；生成式 AI 不限于文本。
- 模型、算法、产品与系统的区别；LLM、RAG、Agent 分别位于什么层面。
- 从数据到参数，再到推理、评测和反馈的完整生命周期。
- 能力边界：预测合理文本不等于事实正确，模型能力不等于系统可靠性。

**验收：**能解释一个文本分类器、聊天模型和带工具的助手分别包含哪些环节。

## M01. 数学与编程 {#m01}

[阅读正文与运行实验]({{ '/chapters/math-and-programming/' | relative_url }})

**目标：**能读懂后续公式与张量运算，不把记忆术语当成掌握。

- Python：函数、类、迭代器、异常、虚拟环境与包管理。
- NumPy 与 PyTorch：张量、形状、广播、设备、数据类型和自动求导。
- 线性代数：向量、矩阵乘法、内积、范数、投影、低秩近似。
- 微积分：导数、偏导、链式法则、梯度与梯度下降。
- 概率与统计：条件概率、贝叶斯公式、期望、方差、抽样与估计。
- 信息论：熵、交叉熵、KL 散度及各自的使用条件。
- 工程基础：文件与数据处理、HTTP/JSON、测试、随机种子与实验记录。

**验收：**能标注矩阵计算的形状，并用自动求导检查一个简单梯度。

## M02. 机器学习 {#m02}

**目标：**建立数据、假设、目标函数、优化与泛化的统一视角。

- 监督、无监督、自监督与强化学习；任务、标签和反馈的区别。
- 回归、分类、聚类、降维；线性模型、树模型和基本基线。
- 特征、模型参数、超参数、损失函数、优化器与决策阈值。
- 训练集、验证集、测试集、交叉验证与数据泄漏。
- 欠拟合、过拟合、正则化、偏差与方差、分布偏移。
- Accuracy、Precision、Recall、F1、ROC/PR、校准及适用条件。
- 数据不平衡、采样策略、误差分析与统计不确定性。

**验收：**能为一个分类任务选择基线、划分数据，并解释指标为何可能误导。

## M03. 神经网络与深度学习 {#m03}

**目标：**理解可训练网络的前向计算与参数更新。

- 神经元、线性层、激活函数、隐藏层、MLP 与计算图。
- 前向传播、链式法则、反向传播与优化器更新。
- SGD、Momentum、Adam/AdamW、学习率、批大小与初始化。
- 梯度消失/爆炸、梯度裁剪、Dropout、残差连接。
- CNN：卷积核、步幅、填充、感受野、特征图与池化。
- RNN：循环状态、时间展开、LSTM、GRU 与长程依赖。
- BatchNorm、LayerNorm、RMSNorm 的归一化维度与适用环境。

**验收：**能逐步解释一次训练迭代，并区分层结构、训练技巧与优化算法。

## M04. NLP 与文本表示 {#m04}

**目标：**理解文本进入模型之前和之后发生了什么。

- 中文分词：分词标准、组合歧义、交集歧义、真歧义与未登录词。
- 词表、特殊 token、未知词和子词；不要混淆词与 token。
- BPE、字节级 BPE、Unigram；词表大小、压缩率、多语言与解码。
- n-gram、词袋、TF-IDF、Word2Vec 与分布式表示。
- Token embedding、位置表示、上下文表示与句向量的区别。
- 相似度、向量空间、检索 embedding 与语义匹配的边界。
- 语言建模：联合概率、条件概率、自回归、掩码预测与困惑度。
- 序列长度、截断、填充、attention mask 与训练样本组织。

**验收：**能解释同一段文本的切词、tokenization 和 embedding 为何不是一回事。

## M05. Transformer 与模型架构 {#m05}

**目标：**能沿数据流解释 Transformer，而不只背模块名称。

- Query、Key、Value、缩放点积注意力、softmax 与张量形状。
- Self-attention、cross-attention、因果掩码与双向注意力。
- MHA、MQA、GQA、MLA 的结构、缓存与计算取舍。
- 位置编码、RoPE、长度外推与长上下文的局限。
- FFN、门控激活、残差连接、Pre-Norm/Post-Norm。
- Encoder-only、Decoder-only、Encoder-Decoder；BERT、GPT、T5 案例。
- 稠密模型与 MoE：路由、共享/路由专家、总参数与激活参数。
- Prefix LM、causal LM、masked LM 与目标函数、掩码的关系；按具体版本核对 UniLM、GLM 示例。

**验收：**能画出一个 Transformer block，并说明哪些信息可以被当前 token 访问。

## M06. 预训练 {#m06}

**目标：**理解基础模型如何在数据、算力、目标与稳定性约束下形成。

- 数据获取与许可、清洗、去重、质量筛选、隐私和数据配比。
- 训练/评测污染、数据溯源、版本管理和可复现性。
- Tokenizer 训练、打包、文档边界、特殊 token 与多语言数据。
- 自回归与掩码目标、loss、困惑度、训练 token 数和 scaling laws。
- 模型规模、数据量、计算预算与训练成本的取舍。
- DP、TP、PP；ZeRO、梯度累积、激活重计算和 checkpoint。
- 注意力计算优化与 FlashAttention；结构设计和 kernel 优化的区别。
- FP32、BF16、FP16、FP8、混合精度与数值稳定性。
- Warmup、学习率衰减、梯度裁剪、loss spike、优化器状态与恢复训练。

**验收：**能解释一次预训练运行的输入、目标、资源瓶颈和失败排查顺序。

## M07. 后训练与能力迁移 {#m07}

**目标：**区分知识、行为、偏好与可验证任务上的能力调整。

- 指令数据、对话格式、SFT 目标、样本质量与数据混合。
- 全量微调与 PEFT 的资源、效果、任务和数据条件。
- LoRA、QLoRA、Adapter、Prefix-Tuning、Prompt/P-Tuning 的不同做法。
- 奖励模型、偏好数据、RLHF/RLAIF 与奖励设计。
- PPO、DPO、GRPO、GSPO 的目标、反馈来源、假设与实现差异。
- 推理模型、可验证奖励、采样、筛选与多阶段后训练。
- 蒸馏、教师数据与学生模型；区分蒸馏、剪枝与量化。
- 灾难性遗忘、奖励投机、过度优化与训练后回归评测。

**验收：**能根据目标和资源选择训练方案，并解释它不能保证解决哪些问题。

## M08. 推理与部署 {#m08}

**目标：**从一次生成请求解释质量、资源、吞吐和延迟。

- Prefill、decode、自回归生成、EOS、停止条件与流式输出。
- Greedy、beam search、temperature、top-k、top-p 与随机性。
- KV Cache、批处理、连续批处理、分页缓存与前缀复用。
- 量化、精度、权重/激活/KV 显存估算及误差影响。
- 推测解码、并行策略与长上下文资源管理。
- 服务引擎、接口、排队、限流、取消、超时和故障恢复。
- TTFT、每 token 延迟、吞吐、并发、负载测试与成本口径。
- 本地实验与托管服务的边界；不依赖付费接口完成基础阅读。

**验收：**能解释一个慢请求的瓶颈，并给出可测量的优化假设。

## M09. 评测、可靠性与安全 {#m09}

**目标：**让技术结论有证据，也有适用边界。

- 离线基准、任务评测、人工评估、线上反馈与回归测试。
- 训练污染、评测集泄漏、数据分布、版本和可比性。
- LLM-as-judge、偏好评测、评分规则、偏差与人工校准。
- 幻觉、引用支持度、不确定性、拒答与校准。
- 提示注入、不可信数据、最小权限、秘密与个人信息保护。
- 输入/输出约束、沙箱、审计、人类审批与高风险操作。
- 公平性、版权与数据许可、模型许可及负责任使用。

**验收：**能为一个模型变更设计评测集，并识别“分数提高但产品变差”的可能原因。

## M10. 多模态 {#m10}

**目标：**建立不同模态的表示、对齐、融合与生成概念。

- 图像 patch、视觉编码器、文本编码器与跨模态表示。
- 对比学习、CLIP 类方法、视觉语言连接与指令微调。
- 图文理解、OCR、文档布局、图表问答与空间信息。
- 音频表示、语音识别、语音合成、视频与时间信息。
- 自回归生成与扩散模型的基本区别。
- 多模态数据质量、评测、幻觉、成本和隐私边界。

**验收：**能分清一个多模态任务需要识别、检索、推理还是生成。

## M11. RAG {#m11}

**目标：**理解检索系统与生成模型的协作及各自失败模式。

- 文档解析、OCR、结构保留、元数据与索引更新。
- 固定、递归、结构/语义切块、重叠及检索单位与上下文单位。
- 稀疏/稠密检索、BM25、embedding、向量索引和近似最近邻。
- 混合检索、过滤、查询改写、重排与上下文组装。
- 证据引用、无答案场景、时效性与访问权限。
- 检索 Recall、排序质量、答案正确性、忠实性与端到端评测。
- 何时用提示词、RAG、微调或组合方案；工具/数据库不是效果保证。

**验收：**能区分未检索到、检索错、上下文丢失与生成无依据四类问题。

## M12. Agent 工程 {#m12}

**目标：**在同步学习中掌握模型之外的执行控制与可靠性。

- 模型、workflow、agent 与 autonomy 的边界。
- 工具定义、结构化输出、schema 校验与调用结果处理。
- 控制循环、状态机、规划、终止条件与预算。
- 短期上下文、长期记忆、检索、摘要与状态持久化。
- MCP 等协议、客户端/服务端、能力发现与权限边界。
- 多 Agent 分工、通信、协调成本与单 Agent 基线。
- 幂等性、重试、检查点、恢复、人类审批和审计。
- 轨迹评测、工具成功率、任务完成率、可观测性与成本。

**验收：**能用一个失败案例解释为什么“模型回答正确”仍不足以完成真实任务。

## M13. 模型案例、实践与面试 {#m13}

**目标：**把原理连接到真实设计，并清楚表达条件与取舍。

- BERT、RoBERTa、XLNet、GPT、T5：用原论文比较结构和训练目标。
- Llama、Qwen、DeepSeek 等公开报告：按版本与发布日期建立案例。
- 稠密/MoE、MLA、MTP、低精度和推理后训练的分层分析。
- 项目阶梯：分类基线 → 小型语言模型实验 → 检索问答 → 带工具的系统。
- 每个项目保留数据许可、环境、运行方式、指标、误差分析与限制。
- 面试组织：简答 → 原理 → 例子 → 边界 → 追问，不把经验说成定律。
- 系统设计：明确需求、评测、容量、安全、成本和替代方案。

**验收：**能复述一篇论文解决的问题，并解释其结果不能直接推广到哪些场景。

## 章节完成标准

- 核心问题、先修知识、直观解释与必要公式齐全。
- 至少有一个可核对的例子；需要代码的章节有运行环境与验证记录。
- 对关键判断给出一手来源，标记版本或核验日期。
- 包含适用条件、常见误区、面试自测及参考回答。
- 引用、站内链接和页面构建通过检查。
- 未运行的实验、尚未核对的来源和未完成章节明确标记，不编造验证结果。